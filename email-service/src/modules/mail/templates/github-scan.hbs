<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        /* General styles for all screen sizes */
        body {
            margin: 0;
            padding: 0;
            width: 100% !important;
            -webkit-text-size-adjust: 100%;
            -ms-text-size-adjust: 100%;
        }

        .container {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            font-family: Arial, sans-serif;
            color: #333;
            background-color: #ffffff;
        }

        .header {
            padding: 20px;
            text-align: center;
            background-color: #f5f5f5;
        }

        .logo {
            width: 50px;
        }

        .content {
            padding: 20px;
            text-align: center;
        }

        .button {
            display: inline-block;
            margin: 20px 0;
            padding: 10px 20px;
            background-color: #4CAF50;
            color: #ffffff !important;
            text-decoration: none;
            border-radius: 5px;
            font-size: 16px;
        }

        .footer {
            padding: 20px;
            font-size: 12px;
            text-align: center;
            color: #666;
        }

        /* Responsive styles for smaller screens */
        @media screen and (max-width: 600px) {
            .container {
                width: 100% !important;
                padding: 0;
            }

            .content {
                padding: 10px;
            }

            .button {
                padding: 15px 20px;
                font-size: 18px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="content">
            {{#if isScanDone}}
            <h2>Your GitHub repository scan is completed</h2>
            <p>Click the button below to view the detailed scan results.</p>
            <a href="{{scanResultLink}}" class="button">View Scan Results</a>

            <!-- Additional Instructions -->
            <p style="margin: 0;">If the button above doesn't work, copy and paste the following link in your browser:
            </p>
            <p style="margin: 0;"><a href="{{scanResultLink}}" target="_blank">{{scanResultLink}}</a></p>
            {{else}}
            <h2>
                Your GitHub repository scan is failed
            </h2>
            {{/if}}
        </div>
        <div class="footer">
            <p>Best,<br>The Development Team</p>
            <p>Need help? If you have any questions, please contact us by email at Jobayer6735@gmail.com.</p>
        </div>
    </div>
</body>

</html>